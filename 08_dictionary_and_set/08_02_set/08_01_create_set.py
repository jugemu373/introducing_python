# 集合は、set()を使うか、1個以上のカンマ区切りの値を{}で囲み、変数に代入して作る
# 集合の要素には、順序はない
# []は、空リストを生成
# {}は、空辞書を生成
# 空集合を生成するためには、set()でなければならない
empty_set = set()
print(empty_set)

even_numbers = {0, 2, 4, 6, 8}
print(even_numbers)

odd_numbers = {1, 3, 5, 7, 9}
print(odd_numbers)
